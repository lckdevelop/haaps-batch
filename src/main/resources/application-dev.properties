# ========================================
# 개발환경 설정 (DEV)
# ========================================

# Oracle Database (DEV)
spring.datasource.url=jdbc:oracle:thin:@10.100.166.55:1523/HDLP1
spring.datasource.username=SC_HAPP
spring.datasource.password=sc_happ!234
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect

# Spring Batch
spring.batch.jdbc.initialize-schema=always
spring.batch.jdbc.table-prefix=BATCH_

# Redis (DEV)
spring.redis.host=10.100.166.55
spring.redis.port=6379
spring.redis.database=0
spring.redis.timeout=5000ms
spring.redis.lettuce.pool.max-active=8
spring.redis.lettuce.pool.max-idle=8
spring.redis.lettuce.pool.min-idle=0

# Oracle DB 타임아웃 (DEV)
spring.datasource.hikari.connection-timeout=5000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5

# FCM (DEV) - 서버 경로 사용
fcm.service-account-key-path=/wasapp/hdlp_meap/push_app/certification/firebase-service-account.json
fcm.project-id=hyundai-happs-dev

# APNS (DEV) - 서버 경로 .p8 파일 사용
apns.key-path=/wasapp/hdlp_meap/push_app/certification/apns-key-dev.p8
apns.environment=sandbox
apns.bundle-id=com.hyundai.happs.dev

# Batch 설정
batch.push.chunk-size=100
batch.push.max-retry=3

# 로깅 레벨 (DEV)
logging.level.com.hyundai.happsbtch=DEBUG
logging.level.org.springframework.batch=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
