<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- 로그 파일 저장 경로 -->
    <property name="LOG_PATH" value="/wasapp/hdlp_meap/push_app/logs" />
    <property name="APP_NAME" value="hdds-ch-happs-btch" />
    <property name="maxHistory" value="30" />
    <property name="maxFileSize" value="100MB" />
    <property name="totalSizeCap" value="10GB" />

    <!-- 콘솔 출력 (모든 환경) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %magenta([%thread]) %highlight([%-5level]) %logger{5} - %msg %n</pattern>
        </encoder>
    </appender>

    <!-- 파일 로그 (서버 환경에서만) -->
    <springProfile name="!local">
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_PATH}/${APP_NAME}.log</file>
            
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <!-- 날짜 + 인덱스 -->
                <fileNamePattern>${LOG_PATH}/LOG_%d{yyyy-MM-dd}_${APP_NAME}.log.%i</fileNamePattern>
                
                <!-- 30일 보관 -->
                <maxHistory>${maxHistory}</maxHistory>
                
                <!-- 파일 최대 크기 -->
                <maxFileSize>${maxFileSize}</maxFileSize>
                
                <!-- 전체 로그 크기 제한 -->
                <totalSizeCap>${totalSizeCap}</totalSizeCap>
            </rollingPolicy>
            
            <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
                <pattern>%-4relative [%thread] %-5level %logger{35} - %msg%n</pattern>
            </encoder>
        </appender>
    </springProfile>

    <!-- 루트 로거 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <springProfile name="!local">
            <appender-ref ref="FILE" />
        </springProfile>
    </root>

</configuration>
